# Enumeration: ImportActions

## Values

| Value | Caption |
|---|---|
| `SynchronizeObjects` | Synchronize objects |
| `SynchronizeOnlyExisting` | Synchronize only existing objects, ignore others |
| `CreateObjects` | Create an object for each row |
| `OnlyCreateNewObjects` | Only create not existing object |
