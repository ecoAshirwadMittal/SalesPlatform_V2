# Microflow Analysis: ACT_MigrateDisplayNameProperties

### Execution Steps:
1. **Search the Database for **EcoATM_PWSMDM.Device** using filter: { Show everything } (Call this list **$DeviceList**)**
2. **Create List
      - Store the result in a new variable called **$BrandList****
3. **Create List
      - Store the result in a new variable called **$CapacityList****
4. **Create List
      - Store the result in a new variable called **$CarrierList****
5. **Create List
      - Store the result in a new variable called **$CategoryList****
6. **Create List
      - Store the result in a new variable called **$ColorList****
7. **Create List
      - Store the result in a new variable called **$GradeList****
8. **Create List
      - Store the result in a new variable called **$ModeList****
9. **Loop Start:** Go through every item in **$list** one-by-one:
   > **(End of Loop)**
10. **Permanently save **$undefined** to the database.**
11. **Permanently save **$undefined** to the database.**
12. **Permanently save **$undefined** to the database.**
13. **Permanently save **$undefined** to the database.**
14. **Permanently save **$undefined** to the database.**
15. **Permanently save **$undefined** to the database.**
16. **Permanently save **$undefined** to the database.**
17. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
