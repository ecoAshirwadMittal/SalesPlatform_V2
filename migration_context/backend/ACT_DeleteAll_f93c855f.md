# Microflow Analysis: ACT_DeleteAll

### Execution Steps:
1. **Search the Database for **EcoATM_EB.ReserveBid** using filter: { Show everything } (Call this list **$ReserveBidList**)**
2. **Search the Database for **EcoATM_EB.ReserveBidSync** using filter: { Show everything } (Call this list **$ReserveBidSyncList**)**
3. **Delete**
4. **Delete**
5. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
