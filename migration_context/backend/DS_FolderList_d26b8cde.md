# Microflow Analysis: DS_FolderList

### Requirements (Inputs):
- **$IncomingEmailConfiguration** (A record of type: Email_Connector.IncomingEmailConfiguration)

### Execution Steps:
1. **Retrieve
      - Store the result in a new variable called **$EmailAccount****
2. **Java Action Call
      - Store the result in a new variable called **$FolderList****
3. **Process successfully completed.**

**Conclusion:** This process sends back a [List] result.
