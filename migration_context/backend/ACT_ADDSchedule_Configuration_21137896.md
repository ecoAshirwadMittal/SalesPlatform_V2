# Microflow Analysis: ACT_ADDSchedule_Configuration

### Execution Steps:
1. **Search the Database for **TaskQueueScheduler.TaskQueue** using filter: { [ShortName='Batch'] } (Call this list **$TaskQueue**)**
2. **Create Object
      - Store the result in a new variable called **$NewSchedule****
3. **Show Page**
4. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
