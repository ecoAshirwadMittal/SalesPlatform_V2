# Microflow Analysis: IVK_deleteAll

### Execution Steps:
1. **Search the Database for **MxModelReflection.MxObjectType** using filter: { Show everything } (Call this list **$allObjTypes**)**
2. **Delete**
3. **Search the Database for **MxModelReflection.Microflows** using filter: { Show everything } (Call this list **$allMicroflows**)**
4. **Delete**
5. **Search the Database for **MxModelReflection.ValueType** using filter: { Show everything } (Call this list **$allValueTypes**)**
6. **Delete**
7. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
