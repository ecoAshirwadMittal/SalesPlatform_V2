# Microflow Analysis: DS_IdleTimeoutConfiguration

### Execution Steps:
1. **Search the Database for **EcoATM_Direct_Theme.IdleTimeoutConfiguration** using filter: { Show everything } (Call this list **$IdleTimeoutConfiguration**)**
2. **Decision:** "IdleTimeOutConfiguration Exist?"
   - If [true] -> Move to: **Finish**
   - If [false] -> Move to: **Activity**
3. **Process successfully completed.**

**Conclusion:** This process sends back a [Object] result.
