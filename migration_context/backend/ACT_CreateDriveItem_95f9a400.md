# Microflow Analysis: ACT_CreateDriveItem

### Requirements (Inputs):
- **$List** (A record of type: Sharepoint.List)

### Execution Steps:
1. **Retrieve
      - Store the result in a new variable called **$Explorer****
2. **Retrieve
      - Store the result in a new variable called **$Drive****
3. **Retrieve
      - Store the result in a new variable called **$Site****
4. **Create Object
      - Store the result in a new variable called **$NewDriveItemContents****
5. **Create Object
      - Store the result in a new variable called **$NewListItem****
6. **Create Object
      - Store the result in a new variable called **$NewDriveItem****
7. **Create Object
      - Store the result in a new variable called **$NewCreateDriveItemHelper****
8. **Show Page**
9. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
