# Microflow Analysis: ACT_GET_CurrentUser

### Execution Steps:
1. **Search the Database for **EcoATM_UserManagement.EcoATMDirectUser** using filter: { [Name=$currentUser/Name] } (Call this list **$EcoATMDirectUser**)**
2. **Process successfully completed.**

**Conclusion:** This process sends back a [Object] result.
