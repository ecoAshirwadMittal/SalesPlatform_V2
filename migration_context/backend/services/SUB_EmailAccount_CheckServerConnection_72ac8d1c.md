# Microflow Analysis: SUB_EmailAccount_CheckServerConnection

### Requirements (Inputs):
- **$EmailAccount** (A record of type: Email_Connector.EmailAccount)

### Execution Steps:
1. **Java Action Call
      - Store the result in a new variable called **$isConnected**** ⚠️ *(This step has a safety catch if it fails)*
2. **Process successfully completed.**

**Conclusion:** This process sends back a [Boolean] result.
