# Microflow Analysis: SUB_GenerateDeposcoPassword

### Requirements (Inputs):
- **$DeposcoConfig** (A record of type: EcoATM_PWSIntegration.DeposcoConfig)

### Execution Steps:
1. **Java Action Call
      - Store the result in a new variable called **$EncodedAuth****
2. **Process successfully completed.**

**Conclusion:** This process sends back a [String] result.
