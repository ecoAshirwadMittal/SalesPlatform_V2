# Java Action: FileFromFileDocument

> Reads contents from a FileDocument and stores it in a file on the local (server) storage.

**Returns:** `Boolean`

## Parameters

| Name | Type | Required |
|---|---|---|
| `targetFile` | String | ✅ |
| `fileDocument` | ConcreteEntity | ✅ |
