# Java Action: commitWithoutEvents

> Commits an object, but without events. N.B. This function is not very useful when called from the model, but it is useful when called from custom Java code.

**Returns:** `Boolean`

## Parameters

| Name | Type | Required |
|---|---|---|
| `subject` | ParameterizedEntity | ✅ |
