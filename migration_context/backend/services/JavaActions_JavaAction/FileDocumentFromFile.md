# Java Action: FileDocumentFromFile

> Loads a file from the local (server) storage and stores it inside a FileDocument.

**Returns:** `Boolean`

## Parameters

| Name | Type | Required |
|---|---|---|
| `file` | String | ✅ |
| `fileDocument` | ConcreteEntity | ✅ |
