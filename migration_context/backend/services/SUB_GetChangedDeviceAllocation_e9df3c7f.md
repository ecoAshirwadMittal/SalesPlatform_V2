# Microflow Analysis: SUB_GetChangedDeviceAllocation

### Requirements (Inputs):
- **$DeviceBuyerList** (A record of type: EcoATM_DA.DeviceBuyer)

### Execution Steps:
1. **Create List
      - Store the result in a new variable called **$DeviceAllocationList****
2. **Loop Start:** Go through every item in **$list** one-by-one:
   > **(End of Loop)**
3. **Process successfully completed.**

**Conclusion:** This process sends back a [List] result.
