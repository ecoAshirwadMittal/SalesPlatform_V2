# Microflow Detailed Specification: ACT_TriggerBidRankingCalculation

### 📥 Inputs (Parameters)
- **$ResetRanking** (Type: Variable)
- **$SchedulingAuction** (Type: AuctionUI.SchedulingAuction)

### ⚙️ Execution Flow (Logic Steps)
1. **Create Variable **$Timer** = `'BidRanking'`**
2. **Call Microflow **Custom_Logging.SUB_Log_StartTimer****
3. **DB Retrieve **AuctionUI.BidRanking**  (Result: **$BidRanking**)**
4. 🔀 **DECISION:** `$BidRanking != empty`
   ➔ **If [false]:**
      1. **Show Message (Error): `Bid Ranking Config does not exist. Kindly Setup.`**
      2. **Call Microflow **Custom_Logging.SUB_Log_EndTimer****
      3. 🏁 **END:** Return `false`
   ➔ **If [true]:**
      1. **Create Variable **$AuctionWeek** = `$SchedulingAuction/Auction_Week_Year`**
      2. 🔀 **DECISION:** `$ResetRanking`
         ➔ **If [false]:**
            1. **Create Variable **$MinBid** = `if($BidRanking/MinBid!=empty) then $BidRanking/MinBid else 0`**
            2. **Create Variable **$MinDisplayRank** = `if($BidRanking/StartingRank!=empty) then $BidRanking/StartingRank else 1`**
            3. **Create Variable **$MaxDisplayRank** = `if($BidRanking/MaxRank!=empty) then $BidRanking/MaxRank else 1000`**
            4. 🔀 **DECISION:** `if($ResetRanking) then $SchedulingAuction/Round=2 else $SchedulingAuction/Round=1`
               ➔ **If [true]:**
                  1. **Create Variable **$RankQueryRound2_ExcludeEB** = `'WITH RankedBids AS ( SELECT bd.id, DENSE_RANK() OVER ( PARTITION BY bd.ecoid, bd.merged_grade ORDER BY bd.submittedbidamount DESC ) AS calculated_rank FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 1 AND bd.submittedbidamount >= '+$MinBid+' ) -- Update both columns simultaneously UPDATE auctionui$biddata AS target SET Round2BidRank = rb.calculated_rank, DisplayRound2BidRank = CASE WHEN rb.calculated_rank <= '+$MaxDisplayRank+' and rb.calculated_rank >= '+$MinDisplayRank+' THEN rb.calculated_rank ELSE NULL END FROM RankedBids rb WHERE target.id = rb.id; '`**
                  2. **Create Variable **$RankQueryRound2_IncludeEB** = `'WITH CombinedBids AS ( SELECT NULL AS biddata_id, t1.productid AS ecoid, t1.grade AS merged_grade, t1.bid AS submitted_amount, 1 AS priority_group FROM ecoatm_eb$reservebid t1 UNION ALL SELECT bd.id AS biddata_id, bd.ecoid, bd.merged_grade, bd.submittedbidamount AS submitted_amount, 2 AS priority_group FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 1 AND bd.submittedbidamount >= '+$MinBid+' ), FinalRanks AS ( SELECT biddata_id, DENSE_RANK() OVER ( PARTITION BY ecoid, merged_grade ORDER BY submitted_amount DESC ) AS final_rank FROM CombinedBids) UPDATE auctionui$biddata AS target SET Round2BidRank = fr.final_rank, DisplayRound2BidRank = CASE WHEN fr.final_rank <= '+$MaxDisplayRank+' and fr.final_rank>= '+$MinDisplayRank+' THEN fr.final_rank ELSE NULL END FROM FinalRanks fr WHERE target.id = fr.biddata_id;'`**
                  3. **Call Microflow **Custom_Logging.SUB_Log_Info****
                  4. **JavaCallAction**
                  5. **Call Microflow **AuctionUI.SUB_UpdateBidRankingDataToSnowflake** (Result: **$SchedulingAuction_DB**)**
                  6. **Call Microflow **Custom_Logging.SUB_Log_EndTimer****
                  7. 🏁 **END:** Return `$IsSuccess_2`
               ➔ **If [false]:**
                  1. **Create Variable **$RankQueryRound3_ExcludeEB** = `'WITH RankedBids AS ( SELECT bd.id, DENSE_RANK() OVER ( PARTITION BY bd.ecoid, bd.merged_grade ORDER BY bd.submittedbidamount DESC ) AS calculated_rank FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 2 AND bd.submittedbidamount >= '+$MinBid+' ) -- Update both columns simultaneously UPDATE auctionui$biddata AS target SET Round3BidRank = rb.calculated_rank, DisplayRound3BidRank = CASE WHEN rb.calculated_rank <= '+$MaxDisplayRank+' and rb.calculated_rank >= '+$MinDisplayRank+' THEN rb.calculated_rank ELSE NULL -- Set to NULL if the rank is 11 or higher END FROM RankedBids rb WHERE target.id = rb.id; '`**
                  2. **Create Variable **$RankQueryRound3_IncludeEB** = `'WITH CombinedBids AS ( SELECT NULL AS biddata_id, t1.productid AS ecoid, t1.grade AS merged_grade, t1.bid AS submitted_amount, 1 AS priority_group FROM ecoatm_eb$reservebid t1 UNION ALL SELECT bd.id AS biddata_id, bd.ecoid, bd.merged_grade, bd.submittedbidamount AS submitted_amount, 2 AS priority_group FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 2 AND bd.submittedbidamount >= '+$MinBid+' ), FinalRanks AS ( SELECT biddata_id, DENSE_RANK() OVER ( PARTITION BY ecoid, merged_grade ORDER BY submitted_amount DESC ) AS final_rank FROM CombinedBids ) UPDATE auctionui$biddata AS target SET Round3BidRank = fr.final_rank, DisplayRound3BidRank = CASE WHEN fr.final_rank <= '+$MaxDisplayRank+' and fr.final_rank>= '+$MinDisplayRank+' THEN fr.final_rank ELSE NULL END FROM FinalRanks fr WHERE target.id = fr.biddata_id;'`**
                  3. **Call Microflow **Custom_Logging.SUB_Log_Info****
                  4. **JavaCallAction**
                  5. **Call Microflow **AuctionUI.SUB_UpdateBidRankingDataToSnowflake** (Result: **$SchedulingAuction_DB**)**
                  6. **Call Microflow **Custom_Logging.SUB_Log_EndTimer****
                  7. 🏁 **END:** Return `$IsSuccess`
         ➔ **If [true]:**
            1. **Create Variable **$QueryToResetRanks** = `if($SchedulingAuction/Round=2) then 'UPDATE auctionui$biddata AS target SET Round2BidRank = NULL, DisplayRound2BidRank = null FROM "auctionui$biddata_bidround" br JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE target.id = br."auctionui$biddataid" -- Join condition to link target row AND sa.auction_week_year = '''+ $AuctionWeek+''' AND target.bidround = 1 and (target.round2Bidrank is not null or target.DisplayRound2BidRank is not null); ' else 'UPDATE auctionui$biddata AS target SET Round3BidRank = null, DisplayRound3BidRank = null FROM "auctionui$biddata_bidround" br JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE target.id = br."auctionui$biddataid" -- Join condition to link target row AND sa.auction_week_year = '''+ $AuctionWeek+''' AND target.bidround = 2 and (target.round3Bidrank is not null or target.DisplayRound3BidRank is not null); '`**
            2. **JavaCallAction**
            3. **Call Microflow **Custom_Logging.SUB_Log_Info****
            4. **Create Variable **$MinBid** = `if($BidRanking/MinBid!=empty) then $BidRanking/MinBid else 0`**
            5. **Create Variable **$MinDisplayRank** = `if($BidRanking/StartingRank!=empty) then $BidRanking/StartingRank else 1`**
            6. **Create Variable **$MaxDisplayRank** = `if($BidRanking/MaxRank!=empty) then $BidRanking/MaxRank else 1000`**
            7. 🔀 **DECISION:** `if($ResetRanking) then $SchedulingAuction/Round=2 else $SchedulingAuction/Round=1`
               ➔ **If [true]:**
                  1. **Create Variable **$RankQueryRound2_ExcludeEB** = `'WITH RankedBids AS ( SELECT bd.id, DENSE_RANK() OVER ( PARTITION BY bd.ecoid, bd.merged_grade ORDER BY bd.submittedbidamount DESC ) AS calculated_rank FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 1 AND bd.submittedbidamount >= '+$MinBid+' ) -- Update both columns simultaneously UPDATE auctionui$biddata AS target SET Round2BidRank = rb.calculated_rank, DisplayRound2BidRank = CASE WHEN rb.calculated_rank <= '+$MaxDisplayRank+' and rb.calculated_rank >= '+$MinDisplayRank+' THEN rb.calculated_rank ELSE NULL END FROM RankedBids rb WHERE target.id = rb.id; '`**
                  2. **Create Variable **$RankQueryRound2_IncludeEB** = `'WITH CombinedBids AS ( SELECT NULL AS biddata_id, t1.productid AS ecoid, t1.grade AS merged_grade, t1.bid AS submitted_amount, 1 AS priority_group FROM ecoatm_eb$reservebid t1 UNION ALL SELECT bd.id AS biddata_id, bd.ecoid, bd.merged_grade, bd.submittedbidamount AS submitted_amount, 2 AS priority_group FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 1 AND bd.submittedbidamount >= '+$MinBid+' ), FinalRanks AS ( SELECT biddata_id, DENSE_RANK() OVER ( PARTITION BY ecoid, merged_grade ORDER BY submitted_amount DESC ) AS final_rank FROM CombinedBids) UPDATE auctionui$biddata AS target SET Round2BidRank = fr.final_rank, DisplayRound2BidRank = CASE WHEN fr.final_rank <= '+$MaxDisplayRank+' and fr.final_rank>= '+$MinDisplayRank+' THEN fr.final_rank ELSE NULL END FROM FinalRanks fr WHERE target.id = fr.biddata_id;'`**
                  3. **Call Microflow **Custom_Logging.SUB_Log_Info****
                  4. **JavaCallAction**
                  5. **Call Microflow **AuctionUI.SUB_UpdateBidRankingDataToSnowflake** (Result: **$SchedulingAuction_DB**)**
                  6. **Call Microflow **Custom_Logging.SUB_Log_EndTimer****
                  7. 🏁 **END:** Return `$IsSuccess_2`
               ➔ **If [false]:**
                  1. **Create Variable **$RankQueryRound3_ExcludeEB** = `'WITH RankedBids AS ( SELECT bd.id, DENSE_RANK() OVER ( PARTITION BY bd.ecoid, bd.merged_grade ORDER BY bd.submittedbidamount DESC ) AS calculated_rank FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 2 AND bd.submittedbidamount >= '+$MinBid+' ) -- Update both columns simultaneously UPDATE auctionui$biddata AS target SET Round3BidRank = rb.calculated_rank, DisplayRound3BidRank = CASE WHEN rb.calculated_rank <= '+$MaxDisplayRank+' and rb.calculated_rank >= '+$MinDisplayRank+' THEN rb.calculated_rank ELSE NULL -- Set to NULL if the rank is 11 or higher END FROM RankedBids rb WHERE target.id = rb.id; '`**
                  2. **Create Variable **$RankQueryRound3_IncludeEB** = `'WITH CombinedBids AS ( SELECT NULL AS biddata_id, t1.productid AS ecoid, t1.grade AS merged_grade, t1.bid AS submitted_amount, 1 AS priority_group FROM ecoatm_eb$reservebid t1 UNION ALL SELECT bd.id AS biddata_id, bd.ecoid, bd.merged_grade, bd.submittedbidamount AS submitted_amount, 2 AS priority_group FROM auctionui$biddata bd JOIN "auctionui$biddata_bidround" br ON br."auctionui$biddataid" = bd.id JOIN "auctionui$bidround_schedulingauction" bsa ON bsa."auctionui$bidroundid" = br."auctionui$bidroundid" JOIN "auctionui$schedulingauction" sa ON sa.id = bsa."auctionui$schedulingauctionid" WHERE sa.auction_week_year = '''+$AuctionWeek+''' AND bd.bidround = 2 AND bd.submittedbidamount >= '+$MinBid+' ), FinalRanks AS ( SELECT biddata_id, DENSE_RANK() OVER ( PARTITION BY ecoid, merged_grade ORDER BY submitted_amount DESC ) AS final_rank FROM CombinedBids ) UPDATE auctionui$biddata AS target SET Round3BidRank = fr.final_rank, DisplayRound3BidRank = CASE WHEN fr.final_rank <= '+$MaxDisplayRank+' and fr.final_rank>= '+$MinDisplayRank+' THEN fr.final_rank ELSE NULL END FROM FinalRanks fr WHERE target.id = fr.biddata_id;'`**
                  3. **Call Microflow **Custom_Logging.SUB_Log_Info****
                  4. **JavaCallAction**
                  5. **Call Microflow **AuctionUI.SUB_UpdateBidRankingDataToSnowflake** (Result: **$SchedulingAuction_DB**)**
                  6. **Call Microflow **Custom_Logging.SUB_Log_EndTimer****
                  7. 🏁 **END:** Return `$IsSuccess`

**Final Result:** This process concludes by returning a [Boolean] value.