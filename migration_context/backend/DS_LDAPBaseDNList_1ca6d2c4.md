# Microflow Analysis: DS_LDAPBaseDNList

### Requirements (Inputs):
- **$LDAPConfiguration** (A record of type: Email_Connector.LDAPConfiguration)

### Execution Steps:
1. **Java Action Call
      - Store the result in a new variable called **$BaseNameList**** ⚠️ *(This step has a safety catch if it fails)*
2. **Process successfully completed.**

**Conclusion:** This process sends back a [List] result.
