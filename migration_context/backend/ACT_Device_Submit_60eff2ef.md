# Microflow Analysis: ACT_Device_Submit

### Execution Steps:
1. **Retrieve
      - Store the result in a new variable called **$DeviceList****
2. **Take the list **$DeviceList**, perform a [Filter] where: { true }, and call the result **$DeviceList_Modified****
3. **Create List
      - Store the result in a new variable called **$PriceList****
4. **Loop Start:** Go through every item in **$list** one-by-one:
   > **(End of Loop)**
5. **Permanently save **$undefined** to the database.**
6. **Permanently save **$undefined** to the database.**
7. **Show Message**
8. **Process successfully completed.**

**Conclusion:** This process sends back a [Void] result.
