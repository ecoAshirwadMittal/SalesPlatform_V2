# Export Mapping: EM_DABuyer

**JSON Structure:** `EcoATM_DA.JSON_DABuyer`

## Mapping Structure

- **Root** (Array)
  - **JsonObject** (Object) → `EcoATM_Integration.DAByBuyer`
    - **ProductId** (Value)
      - Attribute: `EcoATM_Integration.DAByBuyer.PRODUCTID`
    - **Grade** (Value)
      - Attribute: `EcoATM_Integration.DAByBuyer.GRADE`
    - **BuyerCode** (Value)
      - Attribute: `EcoATM_Integration.DAByBuyer.BUYERCODE`
    - **Bid** (Value)
      - Attribute: `EcoATM_Integration.DAByBuyer.BID`
    - **QtyCap** (Value)
      - Attribute: `EcoATM_Integration.DAByBuyer.QUANTITY_ALLOCATED`
