# Microflow Analysis: ACT_GetTimeOffset

### Execution Steps:
1. **Java Action Call
      - Store the result in a new variable called **$offset**** ⚠️ *(This step has a safety catch if it fails)*
2. **Process successfully completed.**

**Conclusion:** This process sends back a [Integer] result.
