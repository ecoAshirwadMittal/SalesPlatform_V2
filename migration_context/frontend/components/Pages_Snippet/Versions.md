# Snippet: Versions

## Widget Tree

- 🔤 **Text**: "Versions: 1.0.0 Initial version 1.1.0 Added ScheduledEvents tab and improved Duration As Mx 9.12 brought changes: ScheduledEvents run on task queue ProcessedQueuedTask has Duration (in milliseconds) 1.2.0 Added pause schedule (to solve ASU_ issue with for contineous deploy procedure) 1.2.1 Fix for cleaning running QueuedAction microflows 1.2.2 Fix for shifting scheduled times (after crash). Added SUB_ScheduleMicroflow to create a scheduled microflow automatically Schedule.LastDuration now measured in milliseconds always 1.2.3 Used SchedulerThrowException in some more microflows. Schedule_Overview page now shows ScheduledEvents more clearly 1.2.4 Removed use of MxModelReflection, so now you can use new microflows instantly (as no MxModelReflection sync is needed anymore) Also provided _SAMPLE AllSchedules batch Please use this when there is a need to split the processing into a number of batches 1.2.5 Fixed SCE_SchedulerQueues becomming inactive on error Added GetObjectById Java action. Use CommunityCommons.getGUID to pass objects to the Scheduler batch process (QueuedAction). Use GetObjectById to get the involved objects when processing starts from queue. Like this you can avoid putting your microflows on a task queue directly (hardcoded in Studio). Using Scheduler batch processes means you can track your batches in Scheduler Running tab 1.2.6 From Ilya: Added TransactionHandling With transaction handling it is possible to process objects batchwise within a single microflow in a way that Mendix can cope with it. Check sample 'batches within a single microflow' to find out how. 1.2.7 Added Schedule/RunAsUser attribute to run a scheduled microflow as a specified user. So you can check what was done by searching the specific user in auditing (no longer all is done by MxAdmin) Added Batch1Clusterwide taskqueue to run microflows sequentially one by one. 1.2.8 Removed ShowByCondition widget"
- 🔤 **Text**: "1.3.0 Made all taskqueue microflows visible in 'Queued/Running' tab, even those marked "Execute this microflow in a Task Queue" in Studio. ScheduledEvent microflows are on 'ScheduledEvent' tab so not on 'Queued/Running' tab but you can adjust this when desired Added fast batch processing based on GUID as batch processing based on BatchSize and Count was too slow for large datasets IMPORTANT: Needs OQL module (Marketplace) to be installed first 1.3.1 Fixed Cal_Schedule_NextRunTime for ScheduleInterval Month and Year."
