# Template for local .env. Copy to `.env` and fill in real values —
# `.env` is gitignored, this file is checked in.
#
#   cp .env.example .env
#
# Consumed by Spring Boot via ${VAR:default} placeholders in
# backend/src/main/resources/application.yml.

# -----------------------------------------------------------------------------
# Snowflake — aggregated inventory sync
# Plan: docs/tasks/aggregated-inventory-snowflake-sync-plan.md
# -----------------------------------------------------------------------------

SNOWFLAKE_ENABLED=false
SNOWFLAKE_JDBC_URL=jdbc:snowflake://ecoatm.snowflakecomputing.com/?db=ECO_DEV&schema=AUCTIONS&TIMEZONE=UTC
SNOWFLAKE_USERNAME=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_POOL_MAX=3
