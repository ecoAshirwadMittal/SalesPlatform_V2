package com.ecoatm.salesplatform.model.partialcredit.enums;

/** Manifest-derived shipment status — Phase 1 reviewer sees this on Missing tab. */
public enum ShipStatus {
    SHIPPED,
    NOT_SHIPPED,
    UNKNOWN
}
