package com.ecoatm.salesplatform.model.partialcredit.enums;

/** A photo is either request-scoped damage evidence or per-line wrong-device evidence. */
public enum PhotoKind {
    DAMAGE,
    WRONG_DEVICE
}
